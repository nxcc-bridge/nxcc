name: Documentation and YAML Quality Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths:
      - "**/README.md"
      - ".github/workflows/*.yml"

jobs:
  quality-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install tools
        run: |
          npm install -g markdown-link-check prettier

      - name: Check Markdown formatting
        run: |
          prettier --check "**/*.md" \
            --config .github/workflows/.prettierrc.json \
            --ignore-pattern "node_modules/**" \
            --ignore-pattern "target/**" \
            --ignore-pattern "dist/**" \
            --ignore-pattern "contracts/evm/dependencies/**" \
            --ignore-pattern "contracts/evm/out/**" \
            --ignore-pattern "contracts/evm/cache/**"

      - name: Check YAML formatting
        run: |
          prettier --check ".github/workflows/*.yml" \
            --config .github/workflows/.prettierrc.json

      - name: Check links in all Markdown files
        run: |
          find . -name "README.md" -not -path "./node_modules/*" -not -path "./.git/*" | \
          xargs -I {} markdown-link-check {} --config .github/workflows/.markdown-link-check.json
