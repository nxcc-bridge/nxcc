{
  "name": "nft-demo",
  "version": "1.0.0",
  "description": "Demo NFT cross-chain mover",
  "private": true,
  "scripts": {
    "build": "tsc && esbuild worker/nft-mover.ts --bundle --outfile=dist/nft-mover.js --format=esm",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "esbuild": "^0.25.9",
    "typescript": "^5.4.5"
  }
}
