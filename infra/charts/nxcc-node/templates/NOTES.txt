Your nXCC application has been deployed.

Release Name: {{ .Release.Name }}
Chart Version: {{ .Chart.Name }}-{{ .Chart.Version }}

COMPONENTS:
- Seed Nodes: {{ .Values.seed.replicaCount }} confidential pod(s) providing p2p bootstrap and high-availability for secrets.
  To check status:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }},app.kubernetes.io/component=seed

- Worker Node: 1 confidential pod handling application logic and ingress traffic.
  To check status:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }},app.kubernetes.io/component=worker

NETWORKING:
{{- if .Values.ingress.enabled }}
- Ingress has been created to route traffic to the worker node.
  HOSTS:
  {{- range .Values.ingress.hosts }}
  - {{ .host }}
  {{- end }}
{{- end }}

{{- if .Values.seed.public.enabled }}
- A public LoadBalancer service has been created for the seed nodes.
  It may take a few minutes for the external IP to be available.
  Get the public IP with:
  kubectl get svc {{ include "nxcc.fullname" . }}-seed-public --watch
{{- end }}
