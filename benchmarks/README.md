

Idle cross-chain workers: 155-161 (about 79MB worth of worker)
CPU-bound (hashing) workers: 1 (number of CPUs not allocated to the platform enclave)
IO-bound (fetching) workers: >500 (limited by memory, not CPU or sockets)
