[workspace]
members = [
    "interface",
    "daemon",
    "enclave",
    "vm/base",
    "vm/workerd",
    "chainlist",
    "attestation",
]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1
