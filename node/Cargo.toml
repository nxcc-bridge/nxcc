[workspace]
members = [
    "interface",
    "daemon",
    "enclave",
    "vm/base",
    "vm/workerd",
    "chainlist",
    "attestation",
]
resolver = "3"

[profile.release]
lto = "thin"
codegen-units = 1
