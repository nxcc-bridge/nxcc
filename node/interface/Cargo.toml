[package]
name = "interface"
version = "0.1.0"
edition = "2024"

[dependencies]
ethers = "2.0.14"
prost = "0.13.5"
serde = { version = "1.0", features = ["derive"] }
tonic = { version = "0.12.3", features = ["transport"] }

[build-dependencies]
tonic-build = "0.12.3"
