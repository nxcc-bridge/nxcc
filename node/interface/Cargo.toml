[package]
name = "interface"
version = "0.1.0"
edition = "2024"

[dependencies]
prost = "0.13.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tonic = { version = "0.12.3", features = ["transport"] }

[build-dependencies]
tonic-build = "0.12.3"
